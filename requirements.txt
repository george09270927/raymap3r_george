# RayMap3R inference requirements (tested configuration).
# PyTorch with CUDA must match your system; install it first from https://pytorch.org.
torch==2.1.1
torchvision==0.16.1
numpy==1.26.4
einops==0.6.1
# transformers must stay <4.46: model.py imports transformers.file_utils (removed
# later) and newest transformers requires torch>=2.4, incompatible with the 2.1.1 pin
transformers==4.45.2
# accelerate is imported by model.py/inference.py but was missing upstream
accelerate==1.0.1
# needed to unpickle the CUT3R checkpoint (its pickled config references omegaconf)
omegaconf
huggingface-hub
safetensors
opencv-python
pillow
imageio
imageio-ffmpeg
scipy
tqdm
plyfile
roma
# visualization only (scripts/): headless rendering + plots
matplotlib
open3d
# The CroCo v2 RoPE CUDA extension (src/croco/models/curope) must be built once:
#   cd src/croco/models/curope && python setup.py build_ext --inplace
